services:
  minister:
    build: .
    container_name: minister-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${ADMIN_PORT:-3000}:${ADMIN_PORT:-3000}"
    volumes:
      - ./data:/app/data

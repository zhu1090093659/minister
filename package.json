{
  "name": "minister",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bot": "bun run packages/bot-server/src/index.ts",
    "mcp": "bun run packages/feishu-mcp/src/index.ts",
    "generate-settings": "bun run scripts/generate-claude-settings.ts",
    "typecheck": "tsc --noEmit",
    "admin:dev": "bun run --cwd packages/admin-ui dev",
    "admin:build": "bun run --cwd packages/admin-ui build"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
